# Pydantic config/validation
pydantic>=2.8
pyyaml>=6.0
python-dotenv>=1.0

# Document IO
python-docx>=1.1.2      # .docx parsing
pymupdf>=1.24.10        # PDF parsing (imported as `fitz`)
unidecode>=1.3.8        # ASCII fallback for weird glyphs

# Retrieval (wired later — ensures future steps need no changes)
sentence-transformers>=3.0.1
faiss-cpu>=1.8.0
rank-bm25>=0.2.2

# API / runtime (wired later)
fastapi>=0.110
uvicorn>=0.30

# Reporting (wired later)
jinja2>=3.1.4
weasyprint>=61.2

# Numerical operations
numpy>=1.26